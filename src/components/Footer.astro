---
import Logo from './Logo.astro';
import { site } from '../data/site';

const year = new Date().getFullYear();
const socials = site.socials.filter((s) => s.url);
---

<footer class="bg-panel text-panel-fg relative mt-28 overflow-hidden rounded-t-[28px] sm:mt-40 sm:rounded-t-[44px]">
  <div class="wrap pt-20 pb-16 text-center sm:pt-28">
    <h2
      class="display mt-6 text-[clamp(2.6rem,7vw,5.5rem)] leading-[0.98] tracking-[-0.035em]"
      data-mask
    >
      <span class="mask"><span class="mi">Let's work</span></span>
      <span class="mask"><span class="mi">better <span class="text-lime">together</span>.</span></span>
    </h2>

    <div class="mt-10 flex flex-col items-center gap-5 sm:mt-12" data-reveal>
      {
        socials.length > 0 && (
          <ul class="text-panel-fg/60 flex flex-wrap justify-center gap-x-6 gap-y-2 text-sm">
            {socials.map((s) => (
              <li>
                <a class="u-link hover:text-panel-fg transition-colors" href={s.url} target="_blank" rel="noopener noreferrer">
                  {s.label} ↗
                </a>
              </li>
            ))}
          </ul>
        )
      }
    </div>
  </div>

  {/* bottom bar — matches the sketch: © | links | Germany */}
  <div class="border-panel-fg/10 mt-16 border-t">
    <div
      class="wrap text-panel-fg/60 flex flex-col items-center justify-between gap-3 py-6 pb-24 text-sm sm:flex-row sm:pb-22"
    >
      <p>{site.name} © {year}</p>
      <p class="flex flex-wrap items-center justify-center gap-x-2 gap-y-1">
        <a href="/imprint/" class="u-link hover:text-panel-fg transition-colors">Impressum</a>
        <span aria-hidden="true">–</span>
        <a href="/privacy/" class="u-link hover:text-panel-fg transition-colors">Datenschutz</a>
        <span aria-hidden="true">–</span>
        <a href="/about/" class="u-link hover:text-panel-fg transition-colors">About me</a>
        <span aria-hidden="true">–</span>
        <a href="#top" class="u-link hover:text-panel-fg transition-colors" data-scroll-top>Back to top</a>
      </p>
      <p class="flex items-center gap-2">
        <svg class="size-4" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.5"></circle>
          <path d="M3 12h18M12 3c2.5 2.6 3.8 5.7 3.8 9S14.5 18.4 12 21c-2.5-2.6-3.8-5.7-3.8-9S9.5 5.6 12 3Z" stroke="currentColor" stroke-width="1.5"></path>
        </svg>
        {site.location}
      </p>
    </div>
  </div>
</footer>
